{
  "name": "rhombiverse",
  "private": true,
  "description": "devDependencies has esbuild, used ONLY by scripts/build.mjs at Vercel build time to minify src/**/*.js for production -- local dev is untouched, still plain ES modules with zero build step (see README.md's 'Running locally').",
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify:i18n": "node scripts/verify-i18n.mjs",
    "verify:rhombis-i18n": "node scripts/verify-rhombis-i18n.mjs",
    "verify:lattice-2d": "node scripts/verify-lattice-2d.mjs",
    "verify:pyrochlore": "node scripts/verify-pyrochlore.mjs",
    "verify:rhombohedra": "node scripts/verify-rhombohedra.mjs",
    "verify:4d": "node scripts/verify-4d.mjs",
    "verify:quasicrystal": "node scripts/verify-quasicrystal.mjs",
    "verify:copy": "node scripts/verify-copy.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
